import React from "react";
import styled from "styled-components";

const Header = () => {
  return <HeaderContainer>Asset Dashboard</HeaderContainer>;
};

export default Header;

const HeaderContainer = styled.div`
  padding: 10px;
  font-size: 20px;
  font-weight: 700;
`;
